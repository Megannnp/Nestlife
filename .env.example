# ═══ NestLife 私有部署配置（复制为 .env 后按需修改） ═══

# 数据根目录（SQLite + 知识库 + 备份）
# 默认：~/.nestlife/data
# NESTLIFE_DATA=/var/lib/nestlife/data

# 监听端口（默认 3100）
# NESTLIFE_PORT=3100

# ── 认证（可选）──────────────────────────────
# 设为 1 启用登录密码；启用时请务必设置强密码
# NESTLIFE_AUTH=1
# NESTLIFE_ADMIN_PASSWORD=changeme

# ── AI 助手（可选）────────────────────────────
# 未配置则 AI 页显示"未接入"，其余功能不受影响
# 也可以用「设置页 → AI 助手接入」直接填写（页面配置优先于此处环境变量）
# 指向任意 OpenAI 兼容网关的 chat/completions 端点（如 DeepSeek / Ollama / OpenClaw）
# NESTLIFE_OPENCLAW_URL=https://api.deepseek.com/v1/chat/completions
# NESTLIFE_OPENCLAW_TOKEN=

# ── 内容室本地文章扫描（可选）──────────────────
# 留空则不扫描本地文章
# NESTLIFE_WECHAT_DIR=/path/to/your/articles

# ── 项目活跃度扫描（可选，cron 用）──────────────
# 格式：id=标签@路径1,路径2;id=标签@路径；留空则跳过本地目录扫描
# NESTLIFE_PROJECT_DIRS="p-app=我的产品@/srv/myapp;p-blog=博客@~/blog"

# ── 异地备份（可选，cron 用）───────────────────
# 指向网盘/挂载的同步目录（如 iCloud Drive、坚果云同步目录）；留空跳过
# NESTLIFE_ICLOUD_DIR=/path/to/cloud-sync/nestlife
